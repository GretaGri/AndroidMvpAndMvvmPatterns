<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.enpassio.androidmvpandmvvmpatterns">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NewsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp_pattern.view.MainActivity" />
        <activity android:name=".mvvm_pattern.view.MainActivity" />
        <activity
            android:name=".MvpByAbhi.view.MainActivity"
            android:launchMode="singleTop" />
        <activity android:name=".mvp_pattern.view.DetailsActivity" />
        <activity
            android:name=".mvvmbyabhi.view.MainActivityMvvmAbhi"
            android:launchMode="singleTop" />
        <activity
            android:name=".mvvmbyabhi.view.FavoriteActivity"
            android:label="Favorite Articles"
            android:parentActivityName=".mvvmbyabhi.view.MainActivityMvvmAbhi" />
    </application>

</manifest>